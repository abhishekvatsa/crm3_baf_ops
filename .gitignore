# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
.pub-cache/
.pub/

# Python inventory tool caches
__pycache__/
*.pyc

# IDE
.idea/
*.iml
.vscode/

# Android signing — CRITICAL: never commit these
key.properties
android/key.properties
*.jks
*.keystore

# OS
.DS_Store
Thumbs.db

# Release artifacts
*.aab
*.apk

# Node / npm
node_modules/
functions/node_modules/

# Firebase emulator + debug output
firestore-debug.log
firebase-debug.log
ui-debug.log
.firebase/

# Stray binaries / build artifacts
isar.dll
*.dll
release_evidence/

release_evidence/
release_gate_*.log

*.p12
*.pfx
release_output/
production_artifacts/

# Local audit, preview and scratch workspaces
output/
outputs/
preview-governed-hierarchy/
tmp/

# Development Firebase override. Written by tool/dev/setup_dev.ps1 locally, and
# by tools/security/codeql/prepare_android.py inside the CodeQL runner, which
# refuses to replace an existing file. It must never be committed.
android/app/src/debug/google-services.json
