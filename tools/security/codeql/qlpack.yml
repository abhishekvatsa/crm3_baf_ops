name: crm3/kotlin-extraction-proof
version: 0.0.0
# Resolve this library from the same pinned CLI bundle via --search-path.
# No pack install/download is needed; do not resolve a registry latest version.
dependencies:
  codeql/java-all: "*"
