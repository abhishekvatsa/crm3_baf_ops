{
  "schemaVersion": 1,
  "findingId": "R-04",
  "sourceStatus": "SOURCE_IMPLEMENTED_CI_REVALIDATION_REQUIRED",
  "registry": {
    "collectionPath": "users/{uid}/notification_installations",
    "documentId": "lowercase UUID v4 persisted per app installation",
    "requiredFields": [
      "schemaVersion",
      "token",
      "platform",
      "updatedAt"
    ],
    "installationSchemaVersion": 1,
    "updatedAtAuthority": "Firestore server timestamp"
  },
  "privacyAndAuthority": {
    "clientReads": "OWNER_POINT_GET_ONLY_NO_LIST",
    "clientWrites": "canonical owner only",
    "parentProfileRequirement": "canonical users/{uid} document",
    "tokenLogging": "PROHIBITED"
  },
  "lifecycle": {
    "sessionStart": "upsert current token",
    "tokenRefresh": "replace token on the same installation document",
    "signOut": "remove only the current installation and retire its local FCM token",
    "installationIdentitySurvivesSignOut": true
  },
  "delivery": {
    "maximumInstallationsReadPerUser": 8,
    "legacyMigrationRead": "users/{uid}.fcmToken",
    "deduplicateByToken": true,
    "staleCleanup": "delete or clear only the exact rejected registration after transactional reread"
  },
  "reArmTriggers": [
    "installation document schema or path changes",
    "notification token lifecycle changes",
    "notification recipient resolution changes",
    "client read or write authority changes",
    "legacy fcmToken migration behavior changes",
    "maximum per-user installation read bound changes"
  ],
  "currentSourceRevalidation": {
    "reason": "Token-conditional sign-out cleanup requires an exact owner read; all collection listing and cross-account reads remain denied.",
    "historicalClosure": "PR 134 closure remains evidence for its original source and authority only.",
    "requiredEvidence": "Current-head Rules emulator and release CI, followed by separate production Rules deployment/readback before activation."
  },
  "evidenceBoundary": {
    "productionDeploymentPerformed": false,
    "deviceDeliveryEvidenceClaimed": false,
    "pilotAuthorizationCreated": false
  }
}
