# Build 28 — release preparation after the system assessment repairs

> **Current applicability — 13 September 2026:** This document preserves the earlier preparation snapshot. Its schema-11 instructions and device observation below are historical. The current runtime compatibility floor is **schema 12**; use the [R04 schema-12 addendum](SYSTEM_ASSESSMENT_R04_COMPATIBILITY_AND_ROLLBACK_2026_09_12.md#local-schema-12-addendum--source-changes-on-13-september-not-release-evidence) for the source-bound approval, compatible repair binary and retained-data upgrade requirements. Startup and subsequent cold-start evidence must establish schema 12 for the exact future signed artifact, using actual device observations rather than copied source expectations. This note does not assert a completed deployment, signed build, device upgrade or distribution.

> **Pre-build repair update:** The [13 September pre-build response](PREBUILD_AUDIT_REMEDIATION_2026_09_13.md) supersedes this snapshot's instruction to preserve unchanged Rules. The new reopen adoption path needs its reviewed, narrowly scoped audit read rule deployed and read back together with the corrected source. Historical deployment approvals and green CI do not certify these later changes.

Status: preparation only. PR #362 merged as `c6038fe7ff3200645ab6a55bbe033eb726f1dfc1` on 12 September 2026. No new backend deployment, build-number reservation, production artifact, device installation or distribution is recorded here.

## Source and completed verification

The application tree is `fe69fb8687d92ac6cd39d0da19d1f4b600b43312`. The merge has the same tree as reviewed PR head `4cfe53763fb4c6138fb3f0498e7ad060595a7e9a`. The PR's [five required checks](https://github.com/abhishekvatsa/crm3_baf_ops/actions/runs/34705749036) and [CodeQL](https://github.com/abhishekvatsa/crm3_baf_ops/actions/runs/34705747057) passed. The distinct [post-merge release gate](https://github.com/abhishekvatsa/crm3_baf_ops/actions/runs/34706367654) also passed all five jobs on this exact merge. [Post-merge CodeQL](https://github.com/abhishekvatsa/crm3_baf_ops/actions/runs/34706367285) passed all three analyses. These results support selecting this source as a deployment baseline; they do not supply the separate deployment and recovery evidence.

The complete local Flutter run passed 2,530 tests, failed none and intentionally skipped one bridge harness; its separate actual-reader campaign passed 23 tests without skips. Full analysis and the 150-check post-code-generation audit passed. Genuine code generation reproduced the committed bindings without changes. The final Functions host pipeline passed 1,375 Jest and 75 Node tests. R04 retains the distinct source hashes for the earlier 392-check emulator campaign and final 14-case V2 run. These checks are source evidence, not production or physical-device evidence.

The local evidence index is `build/review-20260912/local-final-source-verification.json`; it binds the underlying logs by SHA-256. Ignored local logs are not a substitute for permanent release custody.

## Release work still required

1. Record a new exact-source backend decision after the merged source passes its required checks. Preserve the existing `build28-backend-deployment-approval.json`: that immutable decision selects the older `c00c77e2` source and cannot authorize this replacement.
2. Retain and test a compatible backend recovery artifact before activation. Old clients using V1 can already create the new receipt formats after deployment. Reverting unchanged `c00` is not a compatible recovery. The four V2 wrappers and the required original-receipt readers must remain available in rollback or a forward repair.
3. Bind deployment validators to the approved source's exact endpoint set: this candidate has 19 endpoints, including 13 callables, five event/protocol triggers and one scheduler, using 15 existing principals. Keep the historical 15-endpoint receipts and approval anchors intact. Source-derived counts alone do not grant deployment authority.
4. Deploy the reviewed V1 and V2 implementations together under the new decision. Preserve existing IAM, App Check posture, Rules and indexes. Collect actual exact-source endpoint, identity, dependency and capability evidence before binding the client artifact to the backend. Do not manually invoke the scheduler or manufacture business writes for readback.
5. Allocate the unused successor identity through the governed reservation workflow. Build 27 remains the already consumed `1.0.0-rc.17+27` artifact; changed source cannot reuse it. The proposed successor is `1.0.0-rc.18+28`, subject to fresh remote reservation checks and the concrete construction decision.
6. Verify and retain the exact signed APK/AAB and an independently stored private backup. Bind the approved schema version and fingerprint to the exact artifact's Git source. The exact APK's successful governed open validates or creates that fingerprint and commits it with exact marker readback. A separately measured device fingerprint hash is optional because healthy diagnostics do not export one; validate it if supplied, and never copy the expected source hash into a receipt as an invented device observation. Preserve a schema-11-compatible repair binary; never downgrade a marker, remove the submission journal or clear app data to make an older APK open it.
7. Validate an in-place physical-device upgrade and representative accepted, interrupted and recovered business flows on that exact artifact. Capture the first successful `Isar provenance committed: schema=11` startup and retained-data observations, then cold-start again and export existing local diagnostics showing `COMMITTED`, schema 11, `EXISTING_STORE_CANONICAL_CURRENT` and a recognized fingerprint. Diagnostics retain the startup's pre-open inventory, so the first report after upgrading may correctly show schema 10; the second startup checks the persisted schema 11 marker. This uses existing observations without private-storage access or data clearing. ADB's latest read during this work returned no device. Device connection, installed signer/version and business acceptance must be measured again; earlier Build 27 evidence cannot be reused.
8. Complete the chosen distribution channel and record the exact canary/pilot decision with participants and expansion criteria. A source merge, artifact build or successful read-only screen visit does not establish pilot readiness.

## Mixed-version operating window

Use the corrected R04 compatibility matrix. New, unaccepted adjudications from old Build 21/27 clients are intentionally refused because those clients cannot supply the finding version that was reviewed. Supported original accepted receipts remain recoverable. Operators must update and review current evidence before a fresh adjudication; do not replace an uncertain saved command automatically. Current new-client adjudication still uses V1 and is not covered by the campaign's native V2 recovery owner.

Keep other valid V1 work available, subject to corrected business integrity checks. Do not describe the cutover as transparent for every old-client action. The dedicated maintenance-creation Package B integration, explicit component correction workflow, historical business-data repair and unmeasured restore/performance recommendations remain separate from this delivered repair.

## Play Console observation

The existing app entry is `in.co.sail.bsl.crm3.bafops`, developer `5163879421610181326`, app `4975375541039311970`. On 12 September, the live dashboard showed zero closed-test participants and production access unavailable. The signing page showed no upload certificate because no first bundle had been uploaded. Its Digital Asset Links section listed `2F19F489326F92369C98203E103799419AEC7AE112CA92A81B96E361493DC396`; that observation is not proof of every current hybrid certificate. Existing production APKs are governed by signer `6E005FDEFFA62B03FC83177CC8699C4905B7A22B08B2EADC1B69DF0C25F0B47C`. Signing continuity remains unproved.

The change-key warning was inspected and cancelled; no key, access, protection setting or release was changed. The owner's prior policy/signing/export declarations remain accepted; no repeat declaration is required by this plan. Before first distribution through Play, complete and verify the original-key transfer needed for existing installations, then verify the Play-generated APK signer and retained-data update path. Google's [Play App Signing instructions](https://support.google.com/googleplay/android-developer/answer/9842756) distinguish the upload key from the key used to sign installed APKs and describe encrypted original-key transfer.
