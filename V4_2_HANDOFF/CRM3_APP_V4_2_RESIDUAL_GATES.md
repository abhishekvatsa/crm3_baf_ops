# CRM3 v4.2 Residual Gates

## Required for tomorrow’s runnable source proof

1. Restore governed `lib/firebase_options.dart` and `android/app/google-services.json`.
2. Use Flutter 3.44.0 / Dart 3.12.0 / Isar 3.1.0+1.
3. Run authentic `build_runner` generation.
4. Require zero `PROVISIONAL_V4_ISAR_CODEGEN` markers.
5. Pass `flutter analyze`, Flutter tests and debug APK build.
6. Download/start the Firestore emulator and pass Rules/transaction suites.

## Required before repository integration

1. Establish the exact governed repository and branch authority.
2. Apply/reconcile v4.2 on a fresh branch from that authority.
3. Classify every unique delta and preserve current security/release closures.
4. Run CI without weakening the v4 successor policy.

## Required before data migration/cutover

1. Use an authentic current-app Isar database with representative and unsynchronised records.
2. Back up and hash the database.
3. Upgrade using authentic generated schemas.
4. Compare all records, indexes, queues and identity links.
5. Test interruption, rebuild, downgrade refusal and recovery.
6. Audit existing user-document key sets before deploying the stricter schema perimeter.

## Required before controlled pilot

1. Signed Android artifact and Firebase/package/certificate identity proof.
2. App Check registration and signed-client matrix.
3. Physical-device upgrade without uninstall or data clearing.
4. Weak-network, duplicate-command, process-kill and notification tests.
5. Multi-role and revocation matrix.
6. Backend rollback and application recovery drill.
7. Explicit promotion record bound to exact source and artifact hashes.

## Still prohibited by this package

- remote repository mutation;
- Firebase backend deployment;
- production data mutation;
- pilot handout;
- unrestricted field distribution.
